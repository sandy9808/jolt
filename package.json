{
  "name": "jolt",
  "private": true,
  "license": "MIT",
  "scripts": {
    "bootstrap": "pnpm install",
    "build": "pnpm run -r build",
    "lint": "pnpm run -r lint"
  },
  "devDependencies": {
    "pnpm": "^5.4.12"
  }
}
