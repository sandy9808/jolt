import { Component, html } from "jolt";
import style from "./{{component-name}}.css";

const {{component}} = () => {
    return html``;
};

Component.create({
    name: "{{component-name}}",
    styles: [style]
}, {{component}});

export default {{component}};