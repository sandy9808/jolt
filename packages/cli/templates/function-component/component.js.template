import { Component, html } from "jolt";
import style from "./{{component-name}}.css";

export const {{component}} = () => {
    return html``;
};

Component.create({
    name: "{{component-name}}",
    styles: [style]
}, {{component}});